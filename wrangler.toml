name = "area04-backend"
main = "src/index.ts"
compatibility_date = "2026-09-01"
compatibility_flags = ["nodejs_compat"]

[vars]
SUPABASE_URL = "https://sbmvjrfswhbkioynrajd.supabase.co"
AREA = "04"

# Secrets — nunca commitar aqui. Configurar com `wrangler secret put <NOME>`
# ou como secret do repositório no GitHub Actions:
#   SUPABASE_ANON_KEY               (grupo.02, projeto local desta área)
#   AREA04_CROSSPROJECT_DB_URL      (conexão Postgres com o papel restrito
#                                    `area04_backend` no grupo.01 — leitura +
#                                    escrita apenas em certificados.status;
#                                    ver Fase 4 / item 4 do plano de ação)
